# 沙箱联调

## 目标

确认商户服务端代码能够在沙箱环境完成 AI付接口调用，并定位接口、签名、加密、配置类问题。

## 沙箱环境配置

| 项 | 值 |
| --- | --- |
| 网关地址 | **以沙箱提供方给定为准**（当前联调用：`http://11.183.235.246:8080/AIPaySubOrder/<沙箱实例ID>`，渲染后直接改 aipay.env 的 `AIPAY_ENDPOINT_URL` 即可） |
| 接口路径 | 沙箱地址由提供方给定（示例见上），渲染脚本默认拼 `https://fpitest.jd.com/api/pay-ai-agent/<接口名>/{沙箱实例ID}` |
| 沙箱实例 ID | 商户在平台创建沙箱后获得，**由用户自行填写**，追加在接口路径后；实例与接口（案例）的绑定关系由平台配置——若报"未查询到执行实例记录"或"案例接口与项目的关联关系不存在"，说明实例 ID 无效或未绑定该接口，需向平台确认 |
| 京东 SM2 公钥 | 沙箱独立公钥（**test 版**），内置在 `assets/certs/jd-sm2-pub-sandbox.b64`（选 `env=sandbox` 时自动注入，与 pre/prod 共享证书**不同**，无需额外配置） |
| SM3 密钥（secret_key） | 沙箱统一为 **`test`**（内置缺省，无需用户提供） |
| 加签算法 | 标准 HMAC-SM3（与 pre/prod 一致，模板默认）。注意：个别旧网关实例（如 `fpitest.jd.com`）使用拼接式 `SM3(原串+盐)`——若遇到"签名验证不通过"且物料确认无误，可尝试切换拼接式 |
| 京东公钥版本 | **不同沙箱实例绑定的京东公钥可能不同**：当前联调实例绑定 test 版（内置默认）；若解密报 `aks解密过程异常`，向沙箱提供方索要该实例绑定的公钥证书，替换 aipay.env 的 `AIPAY_PUBLIC_KEY` |
| 商户测试私钥 | 内置 `assets/certs/jd-merchant-pfx-sandbox.b64`（CN=金脉智测(AKS00001AKS)）+ 内置密码，商户**无需准备任何证书** |
| 数字字段 | `tradeAmount` / `refundAmount` / `timestamp` 必须为 **JSON 数字类型**（不能加引号），三语言模板已统一数字化 |

> 沙箱环境下商户只需提供**沙箱实例 ID** 与业务参数，其余密钥/证书全部内置。
>
> ⚠️ **当前内置密钥/证书均为沙箱测试环境物料（非最终版）**：后续沙箱生产环境上线后会换发，届时更新 `assets/certs/` 下两份文件即可，工程代码无需改动。

## 输出要求

渲染完成后，向用户说明：

1. 全部可替换的配置项集中在工程根目录的 **`aipay.env`**（KEY=value 格式，含中文注释），切换环境或更换密钥/证书/商户信息时**只改这一个文件**，无需重新生成工程、无需改任何代码。
2. 标注【替换】的项按平台对应页面的值填写；未标注的（如沙箱内置的京东公钥、商户测试私钥、SM3 密钥 `test`）保持原样即可。
3. 修改 aipay.env 后无需编译配置部分，直接重新运行工程即生效。

使用 `scripts/render_server_example.sh` 渲染时：`env=sandbox` + `sandbox_id=<你的沙箱实例ID>`，`base_url` 可省略。

## 行为

1. 使用项目内的沙箱配置或用户提供的沙箱参数；沙箱实例 ID 未提供时，引导用户到平台创建/查看沙箱并获取。
2. 优先跑通下单和支付结果查询；退款链路按商户需求继续验证。
3. 输出调用接口、环境、请求结果、响应摘要和是否通过。
4. 失败时先区分配置缺失、证书密钥、签名、bizContent 加密、HTTP header、网络和业务状态问题。
5. 查询 `reference/api/` 获取字段、状态码和签名加密规则。

## 注意

产品开通不必完成后才做沙箱联调。产品开通审核期间，技术集成和沙箱联调可以继续推进。
